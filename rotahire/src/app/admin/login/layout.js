export const metadata = {
  title: 'Admin Login - RotaHire',
  description: 'Sign in to your RotaHire admin account to manage teams, events, and member data.',
};

export default function AdminLoginLayout({ children }) {
  return children;
}